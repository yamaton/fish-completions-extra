# Auto-generated with h2o

complete -c hexdump -s "b" -l "one-byte-octal" -d "one-byte octal display"
complete -c hexdump -s "c" -l "one-byte-char" -d "one-byte character display"
complete -c hexdump -s "C" -l "canonical" -d "canonical hex+ASCII display"
complete -c hexdump -s "d" -l "two-bytes-decimal" -d "two-byte decimal display"
complete -c hexdump -s "o" -l "two-bytes-octal" -d "two-byte octal display"
complete -c hexdump -s "x" -l "two-bytes-hex" -d "two-byte hexadecimal display"
complete -c hexdump -s "L" -l "color" -d "interpret color formatting specifiers" -x
complete -c hexdump -s "e" -l "format" -d "format string to be used for displaying data" -x
complete -c hexdump -s "f" -l "format-file" -d "file that contains format strings" -r
complete -c hexdump -s "n" -l "length" -d "interpret only length bytes of input" -x
complete -c hexdump -s "s" -l "skip" -d "skip offset bytes from the beginning" -x
complete -c hexdump -s "v" -l "no-squeezing" -d "output identical lines"
complete -c hexdump -s "h" -l "help" -d "display this help"
complete -c hexdump -s "V" -l "version" -d "display version"
