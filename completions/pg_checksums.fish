# Auto-generated with h2o

complete -c pg_checksums -s "c" -l "check" -d "check data checksums (default)"
complete -c pg_checksums -s "d" -l "disable" -d "disable data checksums"
complete -c pg_checksums -s "e" -l "enable" -d "enable data checksums"
complete -c pg_checksums -s "f" -l "filenode" -d "check only relation with specified filenode" -r
complete -c pg_checksums -s "N" -l "no-sync" -d "do not wait for changes to be written safely to disk"
complete -c pg_checksums -s "P" -l "progress" -d "show progress information"
complete -c pg_checksums -s "v" -l "verbose" -d "output verbose messages"
complete -c pg_checksums -s "V" -l "version" -d "output version information, then exit"
complete -c pg_checksums -s "?" -l "help" -d "show this help, then exit"
