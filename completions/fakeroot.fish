# Auto-generated with h2o

complete -c fakeroot -s "l" -l "lib" -d "Specify an alternative wrapper library." -x
complete -c fakeroot -l "faked" -d "Specify an alternative binary to use as faked." -x
complete -c fakeroot -s "s" -d "Save the fakeroot environment to save-file on exit." -r
complete -c fakeroot -s "i" -d "Load a fakeroot environment previously saved using -s from load-file." -r
complete -c fakeroot -s "u" -l "unknown-is-real" -d "Use the real ownership of files previously unknown to fakeroot instead of pretending they are owned by root:root."
complete -c fakeroot -s "b" -d "Specify fd base (TCP mode only)." -r
complete -c fakeroot -s "h" -d "Display help."
complete -c fakeroot -s "v" -d "Display version."
complete -c fakeroot -o "rw-r--r--" -d "0 Jul 2 22:58 hda3" -x
