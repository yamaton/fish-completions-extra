# Auto-generated with h2o

complete -c uuidgen -s "r" -l "random" -d "generate random-based uuid"
complete -c uuidgen -s "t" -l "time" -d "generate time-based uuid"
complete -c uuidgen -s "n" -l "namespace" -d "generate hash-based uuid in this namespace." -x
complete -c uuidgen -s "N" -l "name" -d "generate hash-based uuid from this name" -x
complete -c uuidgen -s "m" -l "md5" -d "generate md5 hash"
complete -c uuidgen -s "s" -l "sha1" -d "generate sha1 hash"
complete -c uuidgen -s "x" -l "hex" -d "interpret name as hex string"
complete -c uuidgen -s "h" -l "help" -d "display this help"
complete -c uuidgen -s "V" -l "version" -d "display version"
