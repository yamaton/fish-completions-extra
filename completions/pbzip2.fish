# Auto-generated with h2o

complete -c pbzip2 -s "1" -d "set BWT block size to 100k (default 900k)"
complete -c pbzip2 -s "2" -d "set BWT block size to 200k (default 900k)"
complete -c pbzip2 -s "3" -d "set BWT block size to 300k (default 900k)"
complete -c pbzip2 -s "4" -d "set BWT block size to 400k (default 900k)"
complete -c pbzip2 -s "5" -d "set BWT block size to 500k (default 900k)"
complete -c pbzip2 -s "6" -d "set BWT block size to 600k (default 900k)"
complete -c pbzip2 -s "7" -d "set BWT block size to 700k (default 900k)"
complete -c pbzip2 -s "8" -d "set BWT block size to 800k (default 900k)"
complete -c pbzip2 -s "9" -d "set BWT block size to 900k (default 900k)"
complete -c pbzip2 -s "b" -d "Block size in 100k steps (default 9 = 900k)" -x
complete -c pbzip2 -s "c" -l "stdout" -d "Output to standard out (stdout)"
complete -c pbzip2 -s "d" -l "decompress" -d "Decompress file"
complete -c pbzip2 -s "f" -l "force" -d "Overwrite existing output file"
complete -c pbzip2 -s "h" -l "help" -d "Print this help message"
complete -c pbzip2 -s "k" -l "keep" -d "Keep input file, don't delete"
complete -c pbzip2 -s "l" -l "loadavg" -d "Load average determines max number processors to use"
complete -c pbzip2 -s "m" -d "Maximum memory usage in 1MB steps (default 100 = 100MB)" -x
complete -c pbzip2 -s "p" -d "Number of processors to use (default: autodetect [12])" -x
complete -c pbzip2 -s "q" -l "quiet" -d "Quiet mode (default)"
complete -c pbzip2 -s "r" -l "read" -d "Read entire input file into RAM and split between processors"
complete -c pbzip2 -s "S" -d "Child thread stack size in 1KB steps (default stack size if unspecified)" -x
complete -c pbzip2 -s "t" -l "test" -d "Test compressed file integrity"
complete -c pbzip2 -s "v" -l "verbose" -d "Verbose mode"
complete -c pbzip2 -s "V" -l "version" -d "Display version info for pbzip2 then exit"
complete -c pbzip2 -s "z" -l "compress" -d "Compress file (default)"
