# Auto-generated with h2o

complete -c sqlite3 -o "bail" -d "Stop after hitting an error."
complete -c sqlite3 -o "batch" -d "Force batch I/O."
complete -c sqlite3 -o "column" -d "Query results will be displayed in a table like form, using whitespace characters to separate the columns and align the output."
complete -c sqlite3 -o "cmd" -d "run command before reading stdin" -x
complete -c sqlite3 -o "csv" -d "Set output mode to CSV (comma separated values)."
complete -c sqlite3 -o "echo" -d "Print commands before execution."
complete -c sqlite3 -o "init" -d "Read and execute commands from file , which can contain a mix of SQL statements and meta-commands." -r
complete -c sqlite3 -o "header" -d "Turn headers on or off."
complete -c sqlite3 -o "noheader" -d "Turn headers on or off."
complete -c sqlite3 -o "help" -d "Show help on options and exit."
complete -c sqlite3 -o "html" -d "Query results will be output as simple HTML tables."
complete -c sqlite3 -o "interactive" -d "Force interactive I/O."
complete -c sqlite3 -o "line" -d "Query results will be displayed with one value per line, rows separated by a blank line."
complete -c sqlite3 -o "list" -d "Query results will be displayed with the separator (|, by default) character between each field value."
complete -c sqlite3 -o "mmap" -d "Set default mmap size to N" -x
complete -c sqlite3 -o "nullvalue" -d "Set string used to represent NULL values." -x
complete -c sqlite3 -o "separator" -d "Set output field separator." -x
complete -c sqlite3 -o "stats" -d "Print memory stats before each finalize."
complete -c sqlite3 -o "version" -d "Show SQLite version."
complete -c sqlite3 -o "vfs" -d "Use name as the default VFS." -x
