# Auto-generated with h2o

complete -c getopt -s "a" -l "alternative" -d "allow long options starting with single -"
complete -c getopt -s "l" -l "longoptions" -d "the long options to be recognized" -x
complete -c getopt -s "n" -l "name" -d "the name under which errors are reported" -x
complete -c getopt -s "o" -l "options" -d "the short options to be recognized" -x
complete -c getopt -s "q" -l "quiet" -d "disable error reporting by getopt(3)"
complete -c getopt -s "Q" -l "quiet-output" -d "no normal output"
complete -c getopt -s "s" -l "shell" -d "set quoting conventions to those of <shell>" -x
complete -c getopt -s "T" -l "test" -d "test for getopt(1) version"
complete -c getopt -s "u" -l "unquoted" -d "do not quote the output"
complete -c getopt -s "h" -l "help" -d "display this help"
complete -c getopt -s "V" -l "version" -d "display version"
