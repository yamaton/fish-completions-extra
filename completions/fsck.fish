# Auto-generated with h2o

complete -c fsck -s "A" -d "check all filesystems"
complete -c fsck -s "C" -d "display progress bar; file descriptor is for GUIs" -r
complete -c fsck -s "l" -d "lock the device to guarantee exclusive access"
complete -c fsck -s "M" -d "do not check mounted filesystems"
complete -c fsck -s "N" -d "do not execute, just show what would be done"
complete -c fsck -s "P" -d "check filesystems in parallel, including root"
complete -c fsck -s "R" -d "skip root filesystem; useful only with '-A'"
complete -c fsck -s "r" -d "report statistics for each device checked; file descriptor is for GUIs" -r
complete -c fsck -s "s" -d "serialize the checking operations"
complete -c fsck -s "T" -d "do not show the title on startup"
complete -c fsck -s "t" -d "specify filesystem types to be checked; <type> is allowed to be a comma-separated list" -r
complete -c fsck -s "V" -d "explain what is being done"
complete -c fsck -s "?" -l "help" -d "display this help"
complete -c fsck -l "version" -d "display version"
