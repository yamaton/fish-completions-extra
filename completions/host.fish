# Auto-generated with h2o

complete -c host -s "a" -d "equivalent to -v -t ANY"
complete -c host -s "A" -d "like -a but omits RRSIG, NSEC, NSEC3"
complete -c host -s "c" -d "specifies query class for non-IN data" -x
complete -c host -s "C" -d "compares SOA records on authoritative nameservers"
complete -c host -s "d" -d "equivalent to -v"
complete -c host -s "l" -d "lists all hosts in a domain, using AXFR"
complete -c host -s "m" -d "set memory debugging flag (trace|record|usage)" -x
complete -c host -s "N" -d "changes the number of dots allowed before root lookup is done" -x
complete -c host -s "p" -d "specifies the port on the server to query" -x
complete -c host -s "r" -d "disables recursive processing"
complete -c host -s "R" -d "specifies number of retries for UDP packets" -x
complete -c host -s "s" -d "a SERVFAIL response should stop query"
complete -c host -s "t" -d "specifies the query type" -x
complete -c host -s "T" -d "enables TCP/IP mode"
complete -c host -s "U" -d "enables UDP mode"
complete -c host -s "v" -d "enables verbose output"
complete -c host -s "V" -d "print version number and exit"
complete -c host -s "w" -d "specifies to wait forever for a reply"
complete -c host -s "W" -d "specifies how long to wait for a reply" -x
complete -c host -s "4" -d "use IPv4 query transport only"
complete -c host -s "6" -d "use IPv6 query transport only"
