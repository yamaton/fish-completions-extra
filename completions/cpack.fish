# Auto-generated with h2o

complete -c cpack -s "G" -d "Override/define CPACK_GENERATOR" -x
complete -c cpack -s "C" -d "Specify the project configuration" -x
complete -c cpack -s "D" -d "Set a CPack variable." -x
complete -c cpack -l "config" -d "Specify the config file." -r
complete -c cpack -l "verbose" -s "V" -d "Enable verbose output"
complete -c cpack -l "trace" -d "Put underlying cmake scripts in trace mode."
complete -c cpack -l "trace-expand" -d "Put underlying cmake scripts in expanded trace mode."
complete -c cpack -l "debug" -d "Enable debug output (for CPack developers)"
complete -c cpack -s "P" -d "Override/define CPACK_PACKAGE_NAME" -x
complete -c cpack -s "R" -d "Override/define CPACK_PACKAGE_VERSION" -x
complete -c cpack -s "B" -d "Override/define CPACK_PACKAGE_DIRECTORY" -r
complete -c cpack -l "vendor" -d "Override/define CPACK_PACKAGE_VENDOR" -x
complete -c cpack -l "help" -o "help" -o "usage" -s "h" -s "H" -d "Print usage information and exit."
complete -c cpack -l "version" -o "version" -d "Print version number and exit." -x
complete -c cpack -l "help-full" -d "Print all help manuals and exit." -x
complete -c cpack -l "help-manual" -d "Print one help manual and exit." -x
complete -c cpack -l "help-manual-list" -d "List help manuals available and exit." -x
complete -c cpack -l "help-command" -d "Print help for one command and exit." -x
complete -c cpack -l "help-command-list" -d "List commands with help available and exit." -x
complete -c cpack -l "help-commands" -d "Print cmake-commands manual and exit." -x
complete -c cpack -l "help-module" -d "Print help for one module and exit." -x
complete -c cpack -l "help-module-list" -d "List modules with help available and exit." -x
complete -c cpack -l "help-modules" -d "Print cmake-modules manual and exit." -x
complete -c cpack -l "help-policy" -d "Print help for one policy and exit." -x
complete -c cpack -l "help-policy-list" -d "List policies with help available and exit." -x
complete -c cpack -l "help-policies" -d "Print cmake-policies manual and exit." -x
complete -c cpack -l "help-property" -d "Print help for one property and exit." -x
complete -c cpack -l "help-property-list" -d "List properties with help available and exit." -x
complete -c cpack -l "help-properties" -d "Print cmake-properties manual and exit." -x
complete -c cpack -l "help-variable" -d "Print help for one variable and exit." -x
complete -c cpack -l "help-variable-list" -d "List variables with help available and exit." -x
complete -c cpack -l "help-variables" -d "Print cmake-variables manual and exit." -x
