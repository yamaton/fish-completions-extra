# Auto-generated with h2o

complete -c pg_archivecleanup -s "d" -d "generate debug output (verbose mode)"
complete -c pg_archivecleanup -s "n" -d "dry run, show the names of the files that would be removed"
complete -c pg_archivecleanup -s "V" -l "version" -d "output version information, then exit"
complete -c pg_archivecleanup -s "x" -d "clean up files if they have this extension" -r
complete -c pg_archivecleanup -s "?" -l "help" -d "show this help, then exit"
