# Auto-generated with h2o

complete -c php -s "a" -d "Run as interactive shell (requires readline extension)"
complete -c php -s "c" -d "Look for php.ini file in this directory" -r
complete -c php -s "n" -d "No configuration (ini) files will be used"
complete -c php -s "d" -d "Define INI entry foo with value 'bar'" -x
complete -c php -s "e" -d "Generate extended information for debugger/profiler"
complete -c php -s "f" -d "Parse and execute <file>." -r
complete -c php -s "h" -d "This help"
complete -c php -s "i" -d "PHP information"
complete -c php -s "l" -d "Syntax check only (lint)"
complete -c php -s "m" -d "Show compiled in modules"
complete -c php -s "r" -d "Run PHP <code> without using script tags <?..?>" -x
complete -c php -s "B" -d "Run PHP <begin_code> before processing input lines" -x
complete -c php -s "R" -d "Run PHP <code> for every input line" -x
complete -c php -s "F" -d "Parse and execute <file> for every input line" -r
complete -c php -s "E" -d "Run PHP <end_code> after processing all input lines" -x
complete -c php -s "H" -d "Hide any passed arguments from external tools."
complete -c php -s "S" -d "Run with built-in web server." -x
complete -c php -s "t" -d "Specify document root <docroot> for built-in web server." -x
complete -c php -s "s" -d "Output HTML syntax highlighted source."
complete -c php -s "v" -d "Version number"
complete -c php -s "w" -d "Output source with stripped comments and whitespace."
complete -c php -s "z" -d "Load Zend extension <file>." -r
complete -c php -l "ini" -d "Show configuration file names"
complete -c php -l "rf" -d "Show information about function <name>." -x
complete -c php -l "rc" -d "Show information about class <name>." -x
complete -c php -l "re" -d "Show information about extension <name>." -x
complete -c php -l "rz" -d "Show information about Zend extension <name>." -x
complete -c php -l "ri" -d "Show configuration for extension <name>." -x
