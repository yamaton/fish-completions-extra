# Auto-generated with h2o

complete -c postgres -s "B" -d "number of shared buffers" -x
complete -c postgres -s "c" -d "set run-time parameter" -x
complete -c postgres -s "C" -d "print value of run-time parameter, then exit" -x
complete -c postgres -s "d" -d "debugging level" -x
complete -c postgres -s "D" -d "database directory" -r
complete -c postgres -s "e" -d "use European date input format (DMY)"
complete -c postgres -s "F" -d "turn fsync off"
complete -c postgres -s "h" -d "host name or IP address to listen on" -x
complete -c postgres -s "i" -d "enable TCP/IP connections"
complete -c postgres -s "k" -d "Unix-domain socket location" -r
complete -c postgres -s "l" -d "enable SSL connections"
complete -c postgres -s "N" -d "maximum number of allowed connections" -x
complete -c postgres -s "p" -d "port number to listen on" -x
complete -c postgres -s "s" -d "show statistics after each query"
complete -c postgres -s "S" -d "set amount of memory for sorts (in kB)" -x
complete -c postgres -s "V" -l "version" -d "output version information, then exit"
complete -c postgres -l "NAME" -d "set run-time parameter" -x
complete -c postgres -l "describe-config" -d "describe configuration parameters, then exit"
complete -c postgres -s "?" -l "help" -d "show this help, then exit"
complete -c postgres -s "f" -d "forbid use of some plan types" -x
complete -c postgres -s "n" -d "do not reinitialize shared memory after abnormal exit"
complete -c postgres -s "O" -d "allow system table structure changes"
complete -c postgres -s "P" -d "disable system indexes"
complete -c postgres -s "t" -d "show timings after each query" -x
complete -c postgres -s "T" -d "send SIGSTOP to all backend processes if one dies"
complete -c postgres -s "W" -d "wait NUM seconds to allow attach from a debugger" -x
complete -c postgres -l "single" -d "selects single-user mode (must be first argument)"
complete -c postgres -s "d" -d "override debugging level" -x
complete -c postgres -s "E" -d "echo statement before execution"
complete -c postgres -s "j" -d "do not use newline as interactive query delimiter"
complete -c postgres -s "r" -d "send stdout and stderr to given file" -r
complete -c postgres -l "boot" -d "selects bootstrapping mode (must be first argument)"
complete -c postgres -l "check" -d "selects check mode (must be first argument)"
