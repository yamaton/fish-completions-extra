# Auto-generated with h2o

complete -c lftp -s "f" -d "execute commands from the file and exit" -r
complete -c lftp -s "c" -d "execute the commands and exit" -x
complete -c lftp -l "norc" -d "don't execute rc files from the home directory"
complete -c lftp -l "help" -d "print this help and exit"
complete -c lftp -l "version" -d "print lftp version and exit"
complete -c lftp -s "e" -d "execute the command just after selecting" -x
complete -c lftp -s "u" -d "use the user/password for authentication" -x
complete -c lftp -s "p" -d "use the port for connection" -x
complete -c lftp -s "s" -d "assign the connection to this slot" -x
complete -c lftp -s "d" -d "switch on debugging mode"
